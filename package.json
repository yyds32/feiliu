{
  "name": "replica-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "dev:full": "prisma migrate dev --name init --preview-feature && next dev",
    "prebuild": "node scripts/generate-sitemap.js",
    "build": "next build",
    "start": "next start",
    "seed": "node prisma/seed.js",
    "migrate": "prisma migrate dev --name init",
    "prisma:generate": "prisma generate",
    "lint": "next lint"
  },
  "dependencies": {
    "@prisma/client": "5.16.0",
    "prisma": "5.16.0",
    "stripe": "12.0.0",
    "next": "14.0.0",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "swr": "2.2.0"
  }
}
